requests>=2.32,<3
networkx>=3.2          # coauthorship ego graphs for reviewer conflicts
numpy>=1.26            # publication-selection model
scipy>=1.11            # meta-analysis, power calculations, back-test

# legacy_plot.py (the superseded matplotlib rendering of the citation graph)
# additionally needs matplotlib. Nothing else in the project does.
